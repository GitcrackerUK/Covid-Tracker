import React from 'react'
import styled from 'styled-components'

const Wrapper = styled.div`
`

export default function SectonHeader(props) {
    return (
        <Wrapper {...props}>

        </Wrapper>
    )
}
